PORT=5000
CONTACT_EMAIL=info@fineraglobal.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://www.fineraglobal.com,https://fineraglobal.com

# Optional — save submissions to MongoDB
# MONGO_URI=mongodb://localhost:27017/contacts

# Optional — use your own SMTP instead of FormSubmit fallback
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=your-app-password
# SMTP_FROM="Finera Global <your@email.com>"
    