# Leave empty to use Vite's /api proxy → http://localhost:5000
# Or set explicitly: VITE_API_BASE_URL=http://localhost:5000
VITE_API_BASE_URL=

# Where FormSubmit delivers contact form emails (browser-side)
VITE_CONTACT_EMAIL=info@fineraglobal.com
